MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority"

# Used for share links (e.g. "https://your-app.vercel.app")
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: set to 1 to allow seed endpoint in production (not recommended)
ALLOW_SEED="0"

